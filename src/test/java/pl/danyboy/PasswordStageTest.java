package pl.danyboy;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class PasswordStageTest {
//    @Test
//    public void shouldGetCoveredPassword() {
//        PasswordStage passwordStage = new PasswordStage();
//        String password = "AAD DDE EE";
//        String coveredPassword = "--- --- --";
//
//        assertEquals(passwordStage.getObscuredPassword(password, null), coveredPassword);
//        assertEquals(passwordStage.getObscuredPassword(password, null).length(), password.length());
//    }

}